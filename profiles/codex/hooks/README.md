# Codex Hooks

This directory contains Codex lifecycle hook artifacts owned by the Codex profile.

- `hooks.json` is the canonical hook configuration template. It registers a `SessionStart` command that emits developer context to activate caveman response style.
