# Shared Artifacts

This directory contains reusable agentic coding artifacts that can support more than one tool profile.

## Layout

- `skills/` contains canonical shared artifacts used by one or more tool profiles.
- `upstreams/` contains upstream snapshots and patches for vendored external artifacts.
