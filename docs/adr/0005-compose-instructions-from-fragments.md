# Compose Instructions from Fragments

Instruction documents are built from ordered **Instruction Fragments** rather than maintained only as one final document. This supports enabling, disabling, testing, and reordering agentic coding guidance while still installing a single managed instruction block into tool-specific files such as `AGENTS.md`.
