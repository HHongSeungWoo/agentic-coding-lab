# Use YAML for Profile Manifests

Profile manifests use YAML because they are human-edited documents that need ordered lists, readable nested structure, and occasional explanatory comments. JSON was rejected because comments are awkward, and TOML was rejected because deeply nested artifact and instruction declarations become harder to scan.
