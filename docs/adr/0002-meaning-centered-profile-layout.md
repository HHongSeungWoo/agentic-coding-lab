# Use Meaning-Centered Profile Layouts

Tool profiles use a **Meaning-Centered Structure** instead of mirroring each tool's local filesystem paths. Bootstrap scripts are responsible for placing instructions, skills, MCP configuration, plugins, and supporting tools into the paths expected by each agent; this keeps the repository's structure tied to agentic coding concepts instead of Codex's current dotfile layout.
