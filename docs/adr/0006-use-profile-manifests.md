# Use Profile Manifests

Each tool profile owns a **Profile Manifest** that declares which artifacts are active, where they install, and how instruction fragments are ordered. This avoids relying on filename sorting or implicit directory scanning for behavior that affects an agent's installed instructions and tools.
